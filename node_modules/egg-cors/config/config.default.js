'use strict';

/**
 * cors default config
 * @member Config#cors
 * @see https://www.npmjs.com/package/kcors#corsoptions
 */
exports.cors = {
		origin: "*",
			allowMethods: "GET, HEAD, PUT, POST, DELETE, PATCH",
			credentials: true
};
config.security = {
	domainWhiteList: ['https://wxweb_test.fasunspace.com:11121/'], //允许访问接口的白名单，例如：http://localhost:8080 *表示均可访问
};
//cors
// config.cors = {
// 	origin: "*",
// 	allowMethods: "GET, HEAD, PUT, POST, DELETE, PATCH",
// 	credentials: true
// }
